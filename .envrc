# 1. Sync the uv environment and install the 'dev' optional dependencies
uv sync --extra dev

# 2. Activate the virtual environment created by uv
source .venv/bin/activate
export MLFLOW_TRACKING_URI=./mlruns